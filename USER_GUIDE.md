# User Guide

## Usage

Clone the repository:

```bash
git clone https://github.com/ifelsebetter/disk-usage-visualizer.git
```

Run the project:

```bash
cargo run
```

Cargo will automatically install dependencies listed in `Cargo.toml`

## Running Tests

To run tests:

```bash
cargo test
```